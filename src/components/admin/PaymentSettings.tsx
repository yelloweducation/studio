
// This component is not used in the non-payment version of the application.
// It's kept as an empty module to satisfy import paths if they existed elsewhere,
// but can be safely deleted if no longer referenced.
export {};
